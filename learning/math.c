#include<stdio.h>
#include<math.h>

int main(){
    
    double a = sqrt(9);
    double b = pow(2, 4);
    double c = round(3.14);
    int d = ceil(3.14);
    int e = floor(3.14);
    double f = fabs(-100); //absolute value
    double g = log(3);
    double h = sin(45);
    double i = cos(45);
    double j = tan(45);

    printf("%.1lf\n", a);
    return 0;
}
